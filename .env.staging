# 预发布环境配置
ENV=staging
NODE_ENV=staging

# 应用端口
APP_PORT=8119

# 数据库配置
DB_PORT=3308
DB_PASSWORD=root123
DB_DATABASE=together_staging

# Redis 配置
REDIS_PORT=6384
REDIS_PASSWORD=

# RustFS 配置
RUSTFS_PORT=8122

# JWT 配置
JWT_SECRET=staging-secret-key-change-in-production
JWT_EXPIRES_IN=7d
